## 1. 本地 Git 初始化

- [x] 1.1 检查并强化 `.gitignore`（忽略 `local.properties`、`**/build`、`*.apk` 等）
- [x] 1.2 初始化 git 仓库并创建首次提交
- [x] 1.3 将默认分支命名为 `main`

## 2. 创建 GitHub 仓库

- [ ] 2.1 在 GitHub 创建新仓库（建议仓库名：`ShareMonitor` 或 `share-monitor`）
- [ ] 2.2 确定推送方式（HTTPS 或 SSH），准备好认证（Token 或 SSH Key）

## 3. 推送到远程

- [ ] 3.1 添加远程 `origin`
- [ ] 3.2 推送 `main` 分支到 GitHub（`git push -u origin main`）
- [ ] 3.3 在 GitHub 上确认代码与目录结构正常，且未包含本地配置/构建产物

